% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effect_edu_film.R
\name{animate_edu_film}
\alias{animate_edu_film}
\title{Animate an educational-film segment}
\usage{
animate_edu_film(
  img,
  n_frames = 48,
  fps = 10,
  weave_deg = 0.1,
  jitter_xy = 0.1,
  flicker = 0.06,
  loop = 0,
  bg = "black",
  seed = NULL,
  write_gif = NULL,
  max_dust = 0.3,
  ...
)
}
\arguments{
\item{img}{magick image (your rasterized ggplot)}

\item{n_frames}{number of frames (e.g., 48)}

\item{fps}{frames per second (e.g., 10)}

\item{weave_deg}{max rotation (deg) for gate weave (per-frame)}

\item{jitter_xy}{max XY translation in pixels (per-frame)}

\item{flicker}{amplitude of brightness flicker (0..0.2 ~ gentle)}

\item{loop}{0 = loop forever (GIF), otherwise number of loops}

\item{bg}{background color used when translating/rotating (usually "black")}

\item{seed}{optional integer; if set, dust/scratches vary per-frame deterministically}

\item{write_gif}{optional file path; if provided, writes the GIF and also returns it}

\item{max_dust}{if not using a fixed \code{dust} in \code{...}, generate random \code{dust} form 0...\code{max_dust}}

\item{...}{passed to \code{patina_edu_film()} (e.g., warmth, grain, dust_polarity, dust_n, scratches)}
}
\value{
an animated magick-image (GIF). If \code{write_gif} is provided, the file is also written.
}
\description{
Builds an animated GIF of a short 16mm instructional clip with gate weave, flicker, and per-frame dust.
Each frame calls \code{patina_edu_film()} with per-frame jitter and seed,
then adds slight brightness flicker and tiny XY weave. Dimensions are preserved.
}
\examples{
\dontrun{
if (requireNamespace("magick", quietly = TRUE)) {
  img <- magick::image_blank(100, 100, "white")
  animate_edu_film(img, n_frames = 5, fps = 2)
}
}
}
